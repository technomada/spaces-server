{
  "name": "spaces-server",
  "version": "1.0.0",
  "description": "Spaces message server in Node JS.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Adam",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.4",
    "express": "^4.18.2",
    "uuid": "^3.3.3"
  }
}
